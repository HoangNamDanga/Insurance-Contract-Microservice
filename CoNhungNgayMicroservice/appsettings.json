{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "ConnectionStrings": {
    "OracleDbConnection": "User Id=INSURANCE_USER;Password=mypassword123;Data Source=localhost:1521/ORCL21PDB1;" //Nếu bạn muốn Code (chạy local) kết nối vào Oracle của Docker: Bạn bắt buộc phải đổi thành: "Data Source=localhost:1522/XEPDB1;".
  },
  "MongoDBSettings": {
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "InsuranceDB",
    "CustomerCollectionName": "Customer",
    "InsuranceTypeCollectionName": "InsuranceType",
    "AgentCollectionName": "Agents",
    "PolicyCollectionName": "Policy",
    "CommissionsCollectionName": "Commissions",
    "ClaimsCollectionName": "Claims",
    "PaymentsCollectionName": "Payments"
  },
  "EmailSettings": { // Khi chạy trong Docker, biến môi trường - EmailSettings__SmtpServer=mailhog trong file compose sẽ tự động đè lên chữ localhost này.
    "SmtpServer": "localhost",
    "SmtpPort": 1025,
    "SenderName": "Bao Hiem Co Nhung Ngay",
    "SenderEmail": "no-reply@conhungngay.com"
  }
}
//chua cau hinh RabbitMq tren local
//Nếu bạn không cấu hình CustomerCollectionName trong docker-compose.yml, .NET sẽ lấy giá trị "mặc định" từ file appsettings.json.
// Lúc nào docker cũng độ ưu tiên cao hơn , và khi các tham số trong docker bỏ trống mà trong appsetting có thì sẽ lấy tham số đó của appsetting
//Nếu docker-compose có ConnectionString, nó sẽ đè lên cái localhost.
//Nếu docker-compose không có CustomerCollectionName, .NET sẽ giữ nguyên giá trị "Customer" lấy từ appsettings.json.
//ConnectionString đã trỏ vào Docker vì chạy trên cổng docker, còn nếu chạy cổng local thì sẽ ăn vào mongoDB local
// nếu các thông số trong docker-compose mà có trong appsetting.json thì docker-compose sẽ ghi đè
//ConnectionString trỏ đi đâu -> Bảng tạo ở đó.
//ConnectionString lấy từ đâu -> Tùy bạn chạy bằng lệnh nào (F5 hay Docker).
//Khi có thay đổi code thì chạy lệnh up để restore code mới lên docker